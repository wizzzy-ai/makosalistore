# TODO
- [ ] Update `frontend/src/components/Nav.js` so the `ConfirmationModal` for logout is rendered regardless of mobile/desktop conditional blocks.
- [ ] Update logout confirm handler to also close the mobile menu (and keep dropdown state consistent).
- [ ] (After code change) Run frontend to verify the logout alert appears on mobile and logout works.

